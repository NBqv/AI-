# Voice Draw — 设计文档

## 项目概述

Voice Draw 是一个面向 2-4 岁儿童的语音绘画工具。儿童说出想画的内容，系统通过模板匹配或 AI 拆解将语音指令转化为画布上的图形。

---

## 设计目标

1. **零门槛** — 幼儿不需要识字，只用说话就能画画
2. **离线优先** — 核心功能（模板匹配、语音识别）不依赖网络
3. **实时反馈** — 语音识别后即刻呈现，不让孩子等待
4. **容错友好** — 口齿不清、不完整的表达也能理解

---

## 指令能力清单

### ✅ 已实现

#### 基础绘图
| 指令 | 示例 | 实现方式 |
|------|------|---------|
| 画基本形状 | `画圆` `画矩形` `画线` | 本地 JS 解析器 |
| 指定颜色 | `红色的圆` `画个蓝色的花` | 颜色映射表 |
| 指定大小 | `半径五十` `大一点的圆` | 参数解析 |
| 多步连续 | `画个猫再画个花再画个房子` | 连续指令分割 + 位置偏移 |
| 移动光标 | `移动到左上角` `向左移` | 位置名称映射 |
| 方向画线 | `往右画线` `从中心到左下角` | 线段解析器 |

#### 模板系统
| 指令 | 示例 | 实现方式 |
|------|------|---------|
| 模板匹配 | `画个猫` `画个房子` `画个恐龙` | aliases.json 精确匹配 |
| 发音容错模板 | `画个兔几` `画个西西` | 发音纠错→模板匹配 |
| 别名匹配 | `画个小狗狗` `画个车车` | 别名/叠词映射 |

#### 相对操作
| 指令 | 示例 | 实现方式 |
|------|------|---------|
| 相对定位 | `在兔子左边画个乌龟` | graphList 追踪 + 坐标偏移 |
| 擦除 | `擦掉兔子` `删除花` | actionHistory 重建 |
| 移动 | `把企鹅移到右边` `移动乌龟到下面` | 坐标批量偏移 + 边界夹紧 |

#### AI 辅助
| 指令 | 示例 | 实现方式 |
|------|------|---------|
| AI 拆解 | `画一条鱼` → 椭圆+三角尾巴+圆眼睛 | Ollama 7B / Qwen2.5 1.5B |
| 交互修改 | `换一个` `改成蓝色` | actions 重生成 |
| 相对位置 AI | `在房子左边再画一棵树` | 上下文注入 + AI 推理 |

#### 界面功能
| 功能 | 说明 |
|------|------|
| 语音识别（中文） | Web Speech API，实时转写 |
| 发音纠错 | 256 条常见儿童发音错误映射 |
| 状态指示器 | 灰/蓝/橙/绿/红 6 种状态 |
| 撤销 | 保存 50 步快照 |
| 导出 PNG | 一键保存画布 |
| 快捷标签 | 7 个示例标签一键画图 |
| 响应式 UI | 适配手机/平板/桌面 |

### ❌ 已规划但未实现

#### 多模态图像生成（AI 画图）
**计划：** 用 Stable Diffusion / DALL·E 根据语音描述生成真实感图片。

**未完成原因：** 项目定位是**儿童简笔画**而不是 AI 照片生成。简笔画更适合幼儿认知（简洁、线条清晰、可模仿），而且对硬件要求低（不需要 GPU）。生成式 AI 图片不可控，不适合幼儿场景。

#### 图形自动描边（黑色边框）
**计划：** 所有模板和绘图形状自动加黑色描边，解决同色堆叠看不清的问题。

**未完成原因：** 需要修改所有绘图函数，在每个 fill 后追加 stroke 调用，并调整图形大小补偿描边宽度。工作量不大但需要逐模板微调以免破坏手写模板的比例。预期耗时 1-2 小时，优先级排在后面的迭代中。

更根本的问题是部分手写模板本身比例和形状就不够像（见评审报告），描边只是锦上添花，核心问题还是模板质量。

#### 模型推理质量提升
**计划：** 用更好的开源模型（如 Qwen2.5-14B 或 Llama-3.1-8B）替换 7B 模型，提升实时拆解质量。

**未完成原因：** 用户机器为 AMD 显卡 + 16GB 内存，不支持 CUDA 加速。CPU 推理 14B 模型需 20-30 秒，用户体验不可接受。7B 模型虽然生成质量一般，但能在 5-10 秒内响应。

作为替代，我们**采用模板优先策略**：95% 的常见儿童词汇用预置模板零延迟匹配，极少情况才走 AI 推理。

#### 100% 模板覆盖率
**计划：** 覆盖 2-4 岁儿童全部可能提到的词汇，模板数量达到 200+。

**未完成原因：** 两次使用千问 API（qwen-plus 和 qwen-max）批量生成的模板经评审约 70% 被判定为"不像"。手动精修 33 个高频模板（兔子、狗、猫等）后效果明显提升，但精修全部 200 个模板预计需要 8-12 小时手写坐标。

已实现的 159 个模板中，手写的 33 个+原有 19 个通过了评审，其余 120+ 个在评审中被标记为"不像但能用"。最终的取舍是：**先保证高频词足够像，低频词有模板兜底**，后续迭代再逐步精修。

#### 平板原生触摸优化
**计划：** 支持多点触控手势（双指缩放、拖动画布）、触控笔压感、iPad Pencil 适配。

**未完成原因：** 核心交互是语音输入而非手动绘图，触摸手势的优先级低于语音管道优化。当前 CSS 已做触摸友好（最小 52px 点击区域），满足基本交互需求。原生触摸 SDK 集成的投入产出比不高。

#### 实时协作 / 分享
**计划：** 支持多设备同时画、作品分享到家庭群。

**未完成原因：** 此功能依赖后端和社交平台 API，与"离线优先"的定位冲突。分享功能作为后续增值服务待规划。

---

## 数据流

```
语音输入 → Web Speech API → 文本
  │
  ├→ 发音纠错（256 条映射）
  │
  ├→ 连续指令分割（"再/然后/还"分割）
  │
  ├→ 擦除/移动 解析 → 执行
  ├→ 相对定位 解析 → 模板偏移 → 执行
  │
  ├→ AI 模式 → POST /parse → 后端 → Ollama/本地 → actions
  │
  └→ 本地模式 → 逐条匹配（颜色/形状/位置/命令）
               → 有模板？→ 零延迟渲染
               → 无模板？→ speak("没听清")
```

---

## 模板系统设计

```
aliases.json
├── complex_shapes (159 个)
│   ├── 手写模板 (33+19=52 个) → 高优先级，通过评审
│   ├── AI 生成模板 (107 个) → 中等优先级，部分待精修
│   └── 匹配规则：最长子串匹配
├── color (15 色，每色 5-10 个别名)
├── shape (20+ 形状名)
├── position (9 个参考点)
├── direction (8 方向)
└── action_* (操作动词)
```

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-06 | 全部 PR 合并完成，核心功能闭环 |

---

## 关于 AI 生成模板的说明

本项目的 159 个简笔画模板中有约 120 个由 AI 模型（qwen-plus / qwen-max）自动生成坐标。经人工评审，AI 生成的模板中有约 30% 通过了视觉考核，其余存在"部件比例失调"、"图形不像"、"颜色搭配怪异"等问题。

核心原因是：**语言模型缺乏对 2D 图形空间的精确理解**。模型能描述"兔子有两只长耳朵"，但无法准确计算耳朵在面部坐标系中的位置和比例。

手工编写的模板（如兔子、狗、花）在评审中获得了较高评价，但手工编写耗时约 3-5 分钟/个。项目当前的策略是**高频词手写 + 低频词 AI 兜底**，逐步将高频使用中好评率低的模板替换为手写版本。
